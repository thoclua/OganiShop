@using Microsoft.AspNetCore.Identity
@model List<IdentityRole>

@{
    var userRole = ViewBag.userRole as List<string>;
    var userId = ViewBag.userId as string;

}
<div class="modal-dialog" role="document">
    <form class="modal-content" action="/User/doSetRole" method="POST">
        <div class="modal-header">
            <h5 class="modal-title">Modal title</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
        <div class="modal-body">
            <div class="form-group row">
                <input type="text" name="userid" value="@userId" style="display:none" />
                <label class=" col-form-label" for="user-phone">Roles:</label>
                <div class="">
                    @foreach (var item in Model)
                    {
                        <label for="arrRole">@item.Name</label>
                        <input type="checkbox" name="arrRole" value="@item.Name" @(userRole.Contains(item.Name) ? "checked":"") />
                    }
                </div>
            </div>
        </div>
        <div class="modal-footer">
            <button type="submit" class="btn btn-primary">Save changes</button>
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        </div>
    </form>
</div>
